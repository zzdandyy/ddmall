server:
  port: 9131
spring:
  application:
    name: management
eureka:
  client:
    fetch-registry: true #是否从eureka中获取信息
    service-url:
      defaultZone: http://127.0.0.1:6868/eureka
  instance:
    prefer-ip-address: true
